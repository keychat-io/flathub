# Generated from flatpak-flutter.yml, do not edit
# Visit the flatpak-flutter project at https://github.com/TheAppgineer/flatpak-flutter
id: com.keychat.io
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.llvm20
command: keychat
finish-args:
  - --socket=fallback-x11
  - --socket=wayland
  - --share=network
  - --device=all
  - --socket=pulseaudio
  - --socket=session-bus
  - --talk-name=org.freedesktop.Secret.Service
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.freedesktop.Portal.*
modules:
  - name: protobuf-compiler
    buildsystem: cmake
    config-opts:
      - -DCMAKE_INSTALL_PREFIX=/app
      - -Dprotobuf_BUILD_TESTS=OFF
    sources:
      - type: archive
        url: https://github.com/protocolbuffers/protobuf/releases/download/v33.1/protobuf-33.1.tar.gz
        sha256: fda132cb0c86400381c0af1fe98bd0f775cb566cb247cdcc105e344e00acc30e
  - name: keychat-app
    buildsystem: simple
    subdir: packages/app
    build-options:
      arch:
        x86_64:
          env:
            BUNDLE_PATH: build/linux/x64/release/bundle
        aarch64:
          env:
            BUNDLE_PATH: build/linux/arm64/release/bundle
      append-path: /usr/lib/sdk/llvm20/bin:/var/lib/rustup/bin:/var/lib/flutter/bin:/app/bin
      prepend-ld-library-path: /usr/lib/sdk/llvm20/lib
      env:
        CARGO_HOME: /run/build/keychat-app/cargo
        RUSTUP_HOME: /var/lib/rustup
        PUB_CACHE: /run/build/keychat-app/.pub-cache
        PROTOC: /app/bin/protoc
        CXXFLAGS: -Wno-error=deprecated-literal-operator
    build-commands:
      - touch ../../cargo/vendor/README.md
      - mkdir ../../cargo/vendor/store-sqlite && cd ../../cargo/vendor/store-sqlite && ln -s ../cashu-wallet-sqlite-aad7f95/migrations migrations
      - echo "a=123" > .env
      - setup-flutter.sh -C .
      - flutter build linux --release --target-platform=linux-x64 --no-pub --verbose
      - install -D $BUNDLE_PATH/keychat /app/bin/keychat
      - cp -r $BUNDLE_PATH/lib /app/bin/lib
      - cp -r $BUNDLE_PATH/data /app/bin/data
    sources:
      - pubspec-sources.json
      - type: git
        url: https://github.com/keychat-io/keychat-app.git
        commit: d7fb114e0e86cb668f5b16afb2fb2e6b74035a97
      - type: patch
        path: cargokit/run_build_tool.sh.patch
        dest: packages/keychat_rust_ffi_plugin/cargokit
      - type: patch
        path: cargokit/run_build_tool.sh.patch
        dest: .pub-cache/hosted/pub.dev/super_native_extensions-0.9.1/cargokit
      - cargo-sources.json
    modules:
      - rustup-1.91.1.json
      - flutter-sdk-3.38.4.json
